<nav>
    <ul>
        <li><a href="/#home">Inicio</a></li>
        <li><a href="/#projects">Proyectos</a></li>
        <li><a href="/#about">Sobre mí</a></li>
        <li><a href="/#contact">Contacto</a></li>
        <li><a href="/Curriculum%20-%20CRISTIAN%20MAIDANA.pdf" download>Currículum</a></li>
        <li class="language-switcher">
            <details>
                <summary aria-label="Cambiar idioma">
                    <svg class="summary-icon" viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2Z" />
                        <path d="M2 12h20" />
                        <path d="M12 2a15.3 15.3 0 0 1 0 20" />
                        <path d="M12 2a15.3 15.3 0 0 0 0 20" />
                    </svg>
                    <svg class="summary-chevron" viewBox="0 0 24 24" aria-hidden="true">
                        <path d="m7 10 5 5 5-5" />
                    </svg>
                </summary>

                <div class="language-menu">
                    <button type="button" class="language-option is-active">Español</button>
                    <button type="button" class="language-option">English</button>
                </div>
            </details>
        </li>
    </ul>
</nav>

<style>
    nav {
        background-color: var(--portfolio-nav-bg);
        backdrop-filter: blur(18px);
        -webkit-backdrop-filter: blur(18px);
        padding: 5px 30px;
        border-radius: 52px;
        width: min(720px, calc(100% - 32px));
        position: fixed;
        top: 20px;
        left: 50%;
        transform: translateX(-50%);
        box-sizing: border-box;
        z-index: 10;
    }
    ul {
        display: flex;
        justify-content: flex-start;
        align-items: center;
        width: 100%;
        gap: 6px;
        margin: 0;
        padding: 0;
        list-style: none;
    }

    li {
        padding: 10px;
        color: var(--portfolio-nav-link);
        font-family: Arial, Helvetica, sans-serif;
        text-align: center;
    }

    li:hover {
        color: var(--portfolio-nav-link-hover);
    }

    a {
        color: inherit;
        text-decoration: none;
    }

    .language-switcher {
        margin-left: auto;
        padding-right: 0;
    }

    details {
        position: relative;
    }

    summary {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 4px;
        width: 52px;
        height: 42px;
        padding: 0 10px;
        border: 1px solid rgba(255, 255, 255, 0.15);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.12);
        color: var(--portfolio-nav-link);
        cursor: pointer;
        list-style: none;
        transition: background-color 0.2s ease, color 0.2s ease, transform 0.2s ease;
    }

    summary::-webkit-details-marker {
        display: none;
    }

    summary:hover,
    details[open] summary {
        background: rgba(255, 255, 255, 0.14);
        color: var(--portfolio-nav-link-hover);
        transform: translateY(-1px);
    }

    .summary-icon,
    .summary-chevron {
        fill: none;
        stroke: currentColor;
        stroke-linecap: round;
        stroke-linejoin: round;
    }

    .summary-icon {
        width: 18px;
        height: 18px;
        stroke-width: 1.8;
    }

    .summary-chevron {
        width: 14px;
        height: 14px;
        stroke-width: 2.2;
    }

    .language-menu {
        position: absolute;
        top: calc(100% + 10px);
        right: 0;
        min-width: 140px;
        display: grid;
        gap: 6px;
        padding: 10px;
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 18px;
        background: rgba(20, 20, 20, 0.88);
        box-shadow: 0 18px 30px rgba(0, 0, 0, 0.22);
        backdrop-filter: blur(18px);
        -webkit-backdrop-filter: blur(18px);
    }

    .language-option {
        border: 0;
        border-radius: 12px;
        padding: 10px 12px;
        background: transparent;
        color: var(--portfolio-nav-link);
        font: inherit;
        text-align: left;
        cursor: pointer;
    }

    .language-option:hover {
        background: rgba(255, 255, 255, 0.08);
        color: var(--portfolio-nav-link-hover);
    }

    .language-option.is-active {
        background: rgba(255, 255, 255, 0.12);
    }

    @media (max-width: 1100px) {
        nav {
            width: min(640px, calc(100% - 24px));
            padding: 8px;
        }

        li {
            padding: 8px 6px;
        }
    }

    @media (max-width: 768px) {
        nav {
            top: 12px;
            border-radius: 24px;
            padding: 8px 10px;
            overflow-x: auto;
        }

        ul {
            width: max-content;
            gap: 2px;
        }

        li {
            padding: 8px 4px;
            font-size: 13px;
        }

        .language-switcher {
            position: sticky;
            right: 0;
            padding-left: 8px;
            background: linear-gradient(90deg, rgba(15, 15, 15, 0) 0%, var(--portfolio-nav-bg) 32%);
        }

        .language-menu {
            right: 0;
        }
    }
</style>
