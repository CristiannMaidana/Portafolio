<div class="home-container">
        <div class="home-copy-wrapper">
            <div class="home-copy">
                <text>JUNIOR DEVELOPER. SYSTEMS STUDENT</text>
                <div class="home-title">
                    <h1>Hola, soy Cristian</h1>
                </div>
                <div class="home-description">
                    <p>Desarrollador junior con experiencia en frontend, backend, arquitectura y diseño visual. 
                        Apasionado por crear experiencias digitales modernas con estética premium y movimiento dinámico.
                    </p>
                </div>
                <div class="home-actions">
                    <a class="btn primary" href="#projects">Ver proyectos</a>
                    <a class="btn secondary" href="#contact" >Contactarme</a>
                </div>
            </div>
        </div>

        <div class="home-terminal-wrapper">
            <div class="container-terminal">
                <div class="terminal">
                    <div class="terminal-toolbar">
                        <div class="circle red"></div>
                        <div class="circle yellow"></div>
                        <div class="circle green"></div>
                    </div>
                    <div class="terminal-divider"></div>
                    <pre class="terminal-code"><code><span class="code-keyword">const</span> developer = &#123;<br>  name: <span class="code-value">"Cristian"</span>,<br>  stack: [<span class="code-stack">"Flutter", "Django", "PostgreSQL"</span>],<br>  focus: <span class="code-value">"Modern Interfaces"</span>,<br>  status: <span class="code-value">"Open to work"</span><br>&#125;;</code></pre>
                    <div class="terminal-stats">
                        <div class="container-data">
                            <p class="titulo">4°</p>
                            <p>Año universitario</p>
                        </div>
                        <div class="container-data">
                            <p class="titulo">Full</p>
                            <p>Stack academico</p>
                        </div>
                        <div class="container-data">
                            <p class="titulo">UI</p>
                            <p>Mobile</p>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

<style>
    .home-container {
        display: flex;
        flex-direction: row;
        gap: 30px;
    }

    .home-copy-wrapper,
    .home-terminal-wrapper {
        display: flex;
        width: 100%;
    }

    .home-terminal-wrapper {
        justify-content: flex-end;
        padding-left: 30px;
        box-sizing: border-box;
    }

    .home-copy {
        display: flex;
        flex-direction: column;
    }

    .home-title {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 90%;
    }

    .home-description {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .home-actions {
        margin-top: 20px;
    }

    .terminal-toolbar {
        display: flex;
        flex-direction: row;
        padding: 20px;
    }

    .terminal-divider {
        height: 1px;
        background: var(--portfolio-divider);
    }

    .terminal-stats {
        display: flex;
        flex-direction: row;
        flex-wrap: nowrap;
        gap: 10px;
        padding: 0 10px 10px;
        box-sizing: border-box;
    }

    .btn{
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 16px 22px;
        border-radius: 16px;
        border: 1px solid transparent;
        font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
        font-size: 16px;
        font-weight: 700;
        text-decoration: none;
        cursor: pointer;
        transition: background-color .3s, color .3s, border-color .3s, transform .3s;
    }
    
    .btn.primary{
        background: linear-gradient(135deg, var(--portfolio-accent-start), var(--portfolio-accent-end));
        color: var(--portfolio-button-text);
    }

    .btn.secondary{
        border-color: var(--portfolio-input-border);
        color: var(--portfolio-text);
        background: var(--portfolio-surface);
        margin-left: 10px;
    }

    .btn:hover {
        transform: translateY(-4px);
    }

    .btn.secondary:hover {
        background: var(--portfolio-button-hover);
    }

    .container-terminal {
        width: 100%;
        max-width: 580px;
        height: 460px;
        box-sizing: border-box;
        background: var(--portfolio-terminal-bg);
        border:1px solid var(--portfolio-terminal-border);
        border-radius: 25px;
        box-shadow: 0 4px 12px var(--portfolio-terminal-shadow);
    }

    .terminal {
        width: calc(100% - 50px);
        height: 300px;
        margin: 25px;
        box-sizing: border-box;
        border-radius: 26px;
        background-color: var(--portfolio-terminal-panel);
    }

    .terminal-code {
        margin: 0;
        padding: 24px;
        padding-right: 0px;
        color: var(--portfolio-code-text);
        font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace;
        font-size: 18px;
        line-height: 1.7;
        white-space: pre-wrap;
    }

    .code-keyword {
        color: var(--portfolio-code-keyword);
    }

    .code-value {
        color: var(--portfolio-code-value);
    }

    .code-stack {
        color: var(--portfolio-code-stack);
    }

    .circle {
        width: 15px;
        height: 15px;
        border-radius: 50%;
        margin-right: 10px;
    }

    .circle.red {
        background: var(--portfolio-window-red);
    }

    .circle.yellow {
        background: var(--portfolio-window-yellow);
    }

    .circle.green {
        background: var(--portfolio-window-green);
    }

    .container-data{
        flex: 1 1 0;
        min-width: 0;
        margin-top: 30px;
        border-radius: 26px;
        background-color: var(--portfolio-stat-surface);
        padding: 10px;
        text-align: center;
        box-sizing: border-box;
    }

    .container-data p {
        margin: 4px 0;
        font-size: clamp(0.85rem, 1.2vw, 1.125rem);
        overflow-wrap: break-word;
    }
    
     .container-data p.titulo{
        font-size: clamp(1.25rem, 2.2vw, 1.875rem);
     } 

    text {
        font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
        font-size: 24px;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: var(--portfolio-accent-alt);
    }

    h1 {
        font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
        font-size: 120px;
        margin-top: 10px;
        margin-bottom: 20px;
    }

    p {
        font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
        font-size: 20px;
        color: var(--portfolio-muted);
    }

    @media (max-width: 1360px) {
        .home-container {
            gap: 20px;
        }

        .container-terminal {
            max-width: 540px;
        }

        .terminal {
            width: calc(100% - 40px);
            margin: 20px;
        }

        .terminal-code {
            padding: 20px;
            font-size: 16px;
            line-height: 1.6;
        }

        .container-data {
            margin-top: 20px;
        }
    }

    @media (max-width: 1200px) {
        .container-terminal {
            max-width: 500px;
        }

        .terminal {
            width: calc(100% - 36px);
            margin: 18px;
        }

        .container-data {
            padding: 8px;
        }
    }

    @media (max-width: 1024px) {
        .home-container {
            flex-direction: column;
        }

        .home-terminal-wrapper {
            justify-content: flex-start;
            padding-left: 0;
        }
    }

    @media (max-width: 768px) {
        .container-terminal {
            height: auto;
        }

        .terminal {
            width: calc(100% - 32px);
            height: auto;
            margin: 16px;
        }

        .terminal-code {
            padding: 20px;
            font-size: 15px;
            line-height: 1.5;
        }

        .container-data {
            margin-top: 16px;
            padding: 10px 8px;
        }
    }
</style>
