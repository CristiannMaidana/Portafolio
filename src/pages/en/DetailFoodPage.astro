---
import LayoutDetailProject from '../../layouts/LayoutDetailProject.astro';
import DetailAlimentosApp from '../../components/DetailFoodApp.astro';
import { getLocale } from '../../i18n/config';
import { ui } from '../../i18n/ui';

const locale = getLocale(Astro.currentLocale);
---

<LayoutDetailProject title={ui[locale].projects.food.name} theme="food">
    <DetailAlimentosApp />
</LayoutDetailProject>
